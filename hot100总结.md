# LeetCode Hot 100 复习总结

## 一、数组

### 哈希表辅助查找

**1. 两数之和** — HashMap存已访问元素，判断`target - x`是否存在。
```java
Map<Integer, Integer> map = new HashMap<>();
for (int i = 0; i < nums.length; i++) {
    int x = nums[i];
    if (map.containsKey(target - x)) {
        return new int[]{map.get(target - x), i};
    }
    map.put(x, i);
}
```

**49. 字母异位词分组** — 排序字符串作为key，HashMap分组。与**438**（滑动窗口+计数数组）同类。
```java
Map<String, List<String>> map = new HashMap<>();
for (String s : strs) {
    char[] ch = s.toCharArray();
    Arrays.sort(ch);
    String key = new String(ch);
    map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
}
return new ArrayList<>(map.values());
```

**560. 和为K的子数组** — 前缀和 + HashMap，「两数之和变种」`k = s[r+1] - s[l]` → `s[l] = s[r+1] - k`。
```java
int[] s = new int[n + 1];
for (int i = 0; i < n; i++) s[i+1] = s[i] + nums[i];
Map<Integer,Integer> cnt = new HashMap<>();
for (int x : s) {
    ans += cnt.getOrDefault(x - k, 0);
    cnt.merge(x, 1, Integer::sum);
}
```

---

### 原地操作 / 技巧

**128. 最长连续序列** — Set去重 → 找起始点(`!set.contains(s-1)`) → while向后扩展，统计最长。
```java
Set<Integer> set = new HashSet<>(); for (int num : nums) set.add(num);
int ans = 1;
for (Integer s : set) {
    if (set.contains(s - 1)) continue;   // 找起始点
    int y = s + 1, cnt = 1;
    while (set.contains(y)) { cnt++; y++; }
    ans = Math.max(cnt, ans);
}
```

**189. 轮转数组** — 原地修改：三次反转（全反转 → 前k反转 → 后n-k反转）。
```java
k %= n;
reverse(nums, 0, n-1);
reverse(nums, 0, k-1);
reverse(nums, k, n-1);
// reverse: while(l<r) { swap(nums[l++], nums[r--]); }
```

**238. 除自身以外数组的乘积** — 前后缀分解，`ans[i] = pre[i] * suf[i]`。
```java
int[] pre = new int[n], suf = new int[n];
pre[0] = suf[n-1] = 1;
for (int i = 1; i < n; i++) pre[i] = pre[i-1] * nums[i-1];
for (int j = n-2; j >= 0; j--) suf[j] = suf[j+1] * nums[j+1];
for (int i = 0; i < n; i++) ans[i] = pre[i] * suf[i];
```

**41. 缺失的第一个正数** — 原地哈希：元素放到下标为`值-1`的位置，`while (nums[i]∈[1,n] && nums[nums[i]-1]!=nums[i]) swap`。
```java
for (int i = 0; i < n; i++) {
    while (nums[i] >= 1 && nums[i] <= n && nums[nums[i]-1] != nums[i]) {
        int j = nums[i] - 1;
        int temp = nums[i]; nums[i] = nums[j]; nums[j] = temp;
    }
}
for (int i = 0; i < n; i++) if (nums[i] != i+1) return i+1;
return n+1;
```

**53. 最大子数组和** — `f[i+1] = max(f[i] + nums[i], nums[i])`（选与不选）。
```java
int[] f = new int[n+1];
int ans = Integer.MIN_VALUE;
for (int i = 0; i < n; i++) {
    f[i+1] = Math.max(f[i] + nums[i], nums[i]);
    ans = Math.max(ans, f[i+1]);
}
```

**56. 合并区间** — 按左端点排序，重叠则取`Math.max(y, 右端点)`。
```java
Arrays.sort(intervals, (p,q) -> p[0] - q[0]);
List<int[]> res = new ArrayList<>();
for (int[] p : intervals) {
    int m = res.size();
    if (m > 0 && res.get(m-1)[1] >= p[0])
        res.get(m-1)[1] = Math.max(p[1], res.get(m-1)[1]);
    else
        res.add(p);
}
```

---

## 二、双指针

### 相向双指针
**11. 盛水最多的容器** — 贪心：移动较短的一边。
```java
int l = 0, r = nums.length-1, ans = 0;
while (l < r) {
    ans = Math.max(ans, Math.min(nums[l], nums[r]) * (r - l));
    if (nums[l] < nums[r]) l++; else r--;
}
```

**42. 接雨水（法二）** — 相向双指针维护`lMax/rMax`，哪边低算哪边。
```java
int lMax = 0, rMax = 0, ans = 0, l = 0, r = n - 1;
while (l < r) {
    lMax = Math.max(lMax, nums[l]);
    rMax = Math.max(rMax, nums[r]);
    if (lMax < rMax) { ans += lMax - nums[l]; l++; }
    else             { ans += rMax - nums[r]; r--; }
}
```

### 前后缀分解
**42. 接雨水（法一）** — `lMax[i]`/`rMax[i]`维护左右最大高度，`ans+=min(lMax,rMax)-height[i]`。同类：**238**。
```java
int[] lMax = new int[n], rMax = new int[n];
int lmax = nums[0], rmax = nums[n-1];
for (int i = 0; i < n; i++) { lmax = Math.max(nums[i], lmax); lMax[i] = lmax; }
for (int j = n-1; j >= 0; j--) { rmax = Math.max(rmax, nums[j]); rMax[j] = rmax; }
for (int i = 0; i < n; i++) ans += Math.min(lMax[i], rMax[i]) - nums[i];
```

**283. 移动零** — 双指针：`l`指向下一个非零元素应放位置。
```java
int l = 0;
for (int r = 0; r < n; r++) {
    if (nums[r] != 0) {
        int tmp = nums[l]; nums[l] = nums[r]; nums[r] = tmp;
        l++;
    }
}
```

**15. 三数之和** — 排序后固定一个数x，在右侧用HashMap找`-x`的两数之和。
```java
Arrays.sort(nums);
for (int i = 0; i < n; i++) {
    int x = nums[i];
    if (i > 0 && x == nums[i-1]) continue;  // 去重
    if (x > 0) break;
    Map<Integer,Integer> map = new HashMap<>();
    for (int j = i+1; j < n; j++) {
        int y = nums[j];
        if (map.containsKey(-x - y)) {
            ans.add(Arrays.asList(x, y, -x - y));
            while (j+1 < n && y == nums[j+1]) j++;  // 去重
        } else { map.put(y, y); }
    }
}
```

---

## 三、滑动窗口

**3. 无重复字符的最长子串** — 右扩展，HashMap计数，重复就缩左边。
```java
int l = 0, ans = 0;
Map<Character,Integer> map = new HashMap<>();
for (int r = 0; r < s.length; r++) {
    char c = s[r];
    map.merge(c, 1, Integer::sum);
    while (map.get(c) > 1) {     // 不满足条件就缩左边
        map.put(s[l], map.get(s[l]) - 1);
        l++;
    }
    ans = Math.max(ans, r - l + 1);
}
```

**438. 找到字符串中所有字母异位词** — 定长滑动窗口+`cnt[26]`计数。
```java
int[] cnt = new int[26];
for (char c : p.toCharArray()) cnt[c - 'a']++;
int l = 0, k = p.length();
for (int r = 0; r < s.length; r++) {
    cnt[s[r] - 'a']--;
    while (cnt[s[r] - 'a'] < 0) { cnt[s[l] - 'a']++; l++; }
    if (r - l + 1 == k) ans.add(l);
}
```

**76. 最小覆盖子串** — 不定长滑动窗口+`isCover`判断覆盖。
```java
int[] sM = new int[128], pM = new int[128];
for (char c : p.toCharArray()) pM[c]++;
int l = 0, ansLeft = -1, ansRight = s.length();
for (int r = 0; r < s.length; r++) {
    sM[s[r]]++;
    while (isCover(sM, pM)) {
        if (r - l < ansRight - ansLeft) { ansLeft = l; ansRight = r; }
        sM[s[l]]--; l++;
    }
}
return ansLeft < 0 ? "" : s.substring(ansLeft, ansRight + 1);
```

### 单调队列
**239. 滑动窗口最大值** — 双向队列维护单调递减，①单调栈退栈 ②过期策略。
```java
Deque<Integer> q = new ArrayDeque<>();
for (int i = 0; i < n; i++) {
    while (!q.isEmpty() && nums[q.getLast()] <= nums[i]) q.removeLast(); // 维护递减
    q.add(i);
    if (q.getFirst() < i - k + 1) q.removeFirst();   // 过期移除
    if (i - k + 1 >= 0) ans[i - k + 1] = nums[q.getFirst()];
}
```

---

## 四、二分查找

### 标准二分 (lowerBound)
**34. 在排序数组中查找元素的第一个和最后一个位置** — `start=lowerBound(target)`，`end=lowerBound(target+1)-1`。
```java
int start = lowerBound(nums, target);
int end = lowerBound(nums, target + 1) - 1;
// lowerBound: 找>=x的第一个元素下标
int l = -1, r = nums.length;
while (l + 1 < r) {
    int mid = l + (r - l) / 2;
    if (nums[mid] < x) l = mid; else r = mid;
}
return r;
```

**74. 搜索二维矩阵** — 先逐行判断target所在行，再对行二分。

### 旋转数组二分
**153. 寻找旋转排序数组中的最小值** — 二分与右端点`nums[r-1]`比较。**33**也用它找分段点。
```java
int l = -1, r = nums.length;
while (l + 1 < r) {
    int mid = l + (r - l) / 2;
    if (nums[mid] > nums[r - 1]) l = mid; else r = mid;
}
return nums[r];
```

**33. 搜索旋转排序数组** — 先二分找最小值(分段点)，再在目标段内二分。
```java
int i = findMin(nums);   // 分段点位置
if (target <= nums[r]) return lowerBound(nums, i, r, target);
else                  return lowerBound(nums, 0, i-1, target);
```

### 中位数二分
**4. 寻找两个正序数组的中位数** — 在较短数组上二分左半段切分位置`i`，推导`j=half-i-2`，维护四条边界。
```java
if (nums1.length > nums2.length) { int[] t = nums1; nums1 = nums2; nums2 = t; } // 短的在nums1
int m=nums1.length, n=nums2.length, half=(m+n+1)/2;
int l=-1, r=m;
while (l+1<r) {
    int i=l+(r-l)/2, j=half-i-2;
    if (i<0 || j+1>=n || nums1[i]<=nums2[j+1]) l=i; else r=i;
}
int i=l, j=half-i-2;
int maxL=Math.max(i>=0?nums1[i]:MIN, j>=0?nums2[j]:MIN);
int minR=Math.min(i+1<m?nums1[i+1]:MAX, j+1<n?nums2[j+1]:MAX);
return (m+n)%2==0 ? (maxL+minR)/2.0 : maxL;
```

---

## 五、栈

### 括号匹配
**20. 有效的括号** — HashMap存右→左映射，左全入栈，右与栈顶消消乐。
```java
Deque<Character> dq = new ArrayDeque<>();
for (char c : s.toCharArray()) {
    if (!map.containsKey(c)) { dq.push(c); }                    // 左括号入
    else if (dq.isEmpty() || dq.pop() != map.get(c)) return false; // 不匹配
}
return dq.isEmpty();
```

### 单调栈
**739. 每日温度** — 单调栈存下标，遇到更大的弹出计算天数差。
```java
Deque<Integer> st = new ArrayDeque<>();
for (int i = 0; i < n; i++) {
    while (!st.isEmpty() && temperatures[i] > temperatures[st.peek()]) {
        int j = st.pop();
        ans[j] = i - j;
    }
    st.push(i);
}
```

**84. 柱状图中最大的矩形** — 单调栈找左右第一个更小的边界，`area=height*(right-left-1)`。
```java
int[] left = new int[n], right = new int[n];
for (int i = 0; i < n; i++) {
    while (!st.isEmpty() && heights[st.peek()] >= heights[i]) st.pop();
    left[i] = st.isEmpty() ? -1 : st.peek();
    st.push(i);
}
st.clear();
for (int r = n - 1; r >= 0; r--) {
    while (!st.isEmpty() && heights[st.peek()] >= heights[r]) st.pop();
    right[r] = st.isEmpty() ? n : st.peek();
    st.push(r);
}
for (int i = 0; i < n; i++) ans = Math.max(ans, heights[i] * (right[i] - left[i] - 1));
```

**42. 接雨水（法三）** — 单调栈遇到更高柱子弹出底部计算积水。
```java
Deque<Integer> st = new ArrayDeque<>();
for (int i = 0; i < n; i++) {
    while (!st.isEmpty() && height[st.peek()] <= height[i]) {
        int bottom = height[st.pop()];
        if (st.isEmpty()) break;
        int l = st.peek();
        ans += Math.min(height[l], height[i]) - bottom;
    }
    st.push(i);
}
```

### 辅助栈
**155. 最小栈** — 栈中存`[val, minVal]`数组，`push`时`minVal=min(val,getMin())`。
```java
Deque<int[]> st = new ArrayDeque<>();
st.push(new int[]{0, Integer.MAX_VALUE});
public void push(int val) { st.push(new int[]{val, Math.min(val, getMin())}); }
public void pop() { st.pop(); }
public int top() { return st.peek()[0]; }
public int getMin() { return st.peek()[1]; }
```

**394. 字符串解码** — 双栈（数字栈+字符串栈），遇到`]`弹出拼接。
```java
Deque<Integer> cntSt = new ArrayDeque<>();
Deque<StringBuilder> strSt = new ArrayDeque<>();
strSt.push(new StringBuilder());
int k = 0;
for (char c : s.toCharArray()) {
    if (Character.isDigit(c)) { k = k * 10 + (c - '0'); }
    else if (c == '[') { cntSt.push(k); k = 0; strSt.push(new StringBuilder()); }
    else if (c == ']') {
        StringBuilder cur = strSt.pop();
        int cnt = cntSt.pop();
        for (int i = 0; i < cnt; i++) strSt.peek().append(cur);
    } else { strSt.peek().append(c); }
}
return strSt.peek().toString();
```

---

## 六、链表

### 反转链表系列
**206. 反转链表** — `pre/cur/nxt`三指针迭代。
```java
ListNode pre = null, cur = head;
while (cur != null) {
    ListNode nxt = cur.next;
    cur.next = pre;
    pre = cur;
    cur = nxt;
}
return pre;
```

**25. K个一组翻转链表** — 分组反转，维护`dum`哨兵和`tail`尾节点。**24**同理。
```java
ListNode dummy = new ListNode(0, head), dum = dummy;
ListNode cur = head;
while (n >= k) {
    n -= k;
    ListNode pre = null;
    for (int i = 0; i < k; i++) {  // 反转k个
        ListNode nxt = cur.next; cur.next = pre; pre = cur; cur = nxt;
    }
    ListNode tail = dum.next;
    tail.next = cur;     // 尾巴接后面未反转部分
    dum.next = pre;      // 哨兵接反转后的头
    dum = tail;          // 哨兵前移
}
return dummy.next;
```

### 快慢指针系列
**141. 环形链表** — 快慢指针`fast=slow`说明有环。
```java
ListNode fast = head, slow = head;
while (fast != null && fast.next != null) {
    fast = fast.next.next;
    slow = slow.next;
    if (fast == slow) return true;
}
return false;
```

**142. 环形链表II** — 快慢指针相遇后，slow与head同步走，相遇点=入环口。数学推导：`a`入环距离，`b`相遇距离，`b=kc`，`b-a`走完一圈到入环口。
```java
while (fast != null && fast.next != null) {
    fast = fast.next.next; slow = slow.next;
    if (fast == slow) {
        while (head != slow) { head = head.next; slow = slow.next; }
        return slow;
    }
}
return null;
```

**148. 排序链表** — 快慢指针找中点 → 归并排序。**23**同理（分治+归并，从拆节点变成拆集合）。
```java
if (head == null || head.next == null) return head;
ListNode head2 = midNode(head);    // 快慢指针断开中点
head = sortList(head); head2 = sortList(head2);
return mergeList(head, head2);
```

**234. 回文链表** — 找中点 → 反转后半段 → 比较。
```java
int m = 0; ListNode cur = head;
while (cur != null) { m++; cur = cur.next; }
m /= 2; cur = head;
while (m-- > 0) cur = cur.next;
ListNode pre = null;
while (cur != null) { ListNode nxt = cur.next; cur.next = pre; pre = cur; cur = nxt; }
while (pre != null) {
    if (head.val != pre.val) return false;
    head = head.next; pre = pre.next;
}
return true;
```

### 双指针
**160. 相交链表** — A走完跳B，B走完跳A，相遇点=交点。
```java
ListNode A = listA, B = listB;
while (A != B) {
    A = A != null ? A.next : listB;
    B = B != null ? B.next : listA;
}
return A;
```

**21. 合并两个有序链表** — 双指针归并+dummy哨兵。
```java
ListNode dummy = new ListNode(0), cur = dummy;
while (pos1 != null && pos2 != null) {
    if (pos1.val <= pos2.val) { cur.next = pos1; pos1 = pos1.next; }
    else                      { cur.next = pos2; pos2 = pos2.next; }
    cur = cur.next;
}
cur.next = pos1 == null ? pos2 : pos1;
return dummy.next;
```

**23. 合并K个升序链表** — 分治+归并。
```java
return mergeKlink(links, 0, links.length);
// mergeKlink: 递归二分 divide & conquer，最后 mergeList(a,b)
```

### 数学 / 遍历
**2. 两数相加** — 模拟竖式加法，维护进位`cont`。
```java
ListNode dummy = new ListNode(0), cur = dummy; int cont = 0;
while (head1 != null || head2 != null) {
    int x = head1==null ? 0 : head1.val, y = head2==null ? 0 : head2.val;
    int sum = x + y + cont;
    cont = sum / 10;
    cur.next = new ListNode(sum % 10); cur = cur.next;
    if (head1 != null) head1 = head1.next;
    if (head2 != null) head2 = head2.next;
}
if (cont != 0) cur.next = new ListNode(cont);
return dummy.next;
```

### 链表复制
**138. 随机链表的复制** — ①HashMap映射 ②交错插入。
```java
// 法一：HashMap映射
Map<Node,Node> map = new HashMap<>();
for (Node cur=head; cur!=null; cur=cur.next) map.put(cur, new Node(cur.val));
for (Node cur=head; cur!=null; cur=cur.next) {
    map.get(cur).next   = cur.next   !=null ? map.get(cur.next)   : null;
    map.get(cur).random = cur.random !=null ? map.get(cur.random) : null;
}

// 法二：交错插入 — 每个节点后插入副本 → 设random → 拆链
while (cur != null) { cur.next = new Node(cur.val, cur.next); cur = cur.next.next; }
while (cur != null) { if(cur.random!=null) cur.next.random=cur.random.next; cur=cur.next.next; }
// 拆链...
```

---

## 七、二叉树

### 遍历
**94. 二叉树的中序遍历** — `dfs(left) → add(val) → dfs(right)`。
```java
void dfs(List<Integer> ans, TreeNode node) {
    if (node == null) return;
    dfs(ans, node.left);
    ans.add(node.val);
    dfs(ans, node.right);
}
```

**102. 二叉树的层序遍历** — BFS队列，按层大小`q.size()`分批。
```java
Deque<TreeNode> q = new ArrayDeque<>(); q.offer(root);
while (!q.isEmpty()) {
    int n = q.size();
    List<Integer> res = new ArrayList<>(n);
    while (n-- > 0) {
        TreeNode cur = q.poll(); res.add(cur.val);
        if (cur.left != null) q.offer(cur.left);
        if (cur.right != null) q.offer(cur.right);
    }
    ans.add(res);
}
```

### 判定 / 验证
**98. 验证二叉搜索树** — ①前序限界`dfs(node, min, max)`；②中序递增。
```java
// 前序限界
boolean dfs(TreeNode root, int min, int max) {
    if (root == null) return true;
    if (root.val <= min || root.val >= max) return false;
    return dfs(root.left, min, root.val) && dfs(root.right, root.val, max);
}

// 中序递增
int pre = Integer.MIN_VALUE;
boolean dfs(TreeNode root) {
    if (root == null) return true;
    if (!dfs(root.left)) return false;
    if (root.val <= pre) return false;
    pre = root.val;
    return dfs(root.right);
}
```

**101. 对称二叉树** — 递归比较`isMirror(a.left, b.right) && isMirror(a.right, b.left)`。
```java
boolean isMirror(TreeNode a, TreeNode b) {
    if (a == null && b == null) return true;
    if (a == null || b == null || a.val != b.val) return false;
    return isMirror(a.left, b.right) && isMirror(a.right, b.left);
}
```

**236. 二叉树的最近公共祖先** — 后序遍历，左右都找到→返回root，只在一侧→返回该侧。
```java
Integer lowestFind(TreeNode root, int p, int q) {
    if (root == null) return null;
    if (root.val == p || root.val == q) return root.val;  // 找到就返回
    Integer left  = lowestFind(root.left,  p, q);
    Integer right = lowestFind(root.right, p, q);
    if (left != null && right != null) return root.val;   // 分散在两边
    return left != null ? left : right;                   // 只在一侧
}
```

### 递归 + 深度
**104. 二叉树的最大深度** — `max(maxDepth(left), maxDepth(right)) + 1`。

**543. 二叉树的直径** — `ans=max(ans, left+right)`，返回`max(left,right)+1`。
```java
int dfs(TreeNode node) {
    if (node == null) return 0;
    int l = dfs(node.left), r = dfs(node.right);
    ans = Math.max(ans, l + r);    // 更新最大直径
    return Math.max(l, r) + 1;     // 返回最大深度
}
```

**226. 翻转二叉树** — 后序递归交换左右子树。
```java
if (root == null) return null;
invertTree(root.left); invertTree(root.right);
TreeNode tmp = root.right; root.right = root.left; root.left = tmp;
return root;
```

### 构造
**105. 从前序与中序遍历构造二叉树** — `pre[根,左,右] + mid[左,根,右]`，在中序定位根，递归构造。
```java
TreeNode dfs(int[] pre, int preL, int preR, int[] mid, int midL, int midR) {
    if (preL > preR) return null;
    int rootVal = pre[preL];
    int k = midL; while (mid[k] != rootVal) k++;
    int leftSize = k - midL;
    TreeNode root = new TreeNode(rootVal);
    root.left  = dfs(pre, preL+1, preL+leftSize, mid, midL, k-1);
    root.right = dfs(pre, preL+leftSize+1, preR, mid, k+1, midR);
    return root;
}
```

**108. 将有序数组转换为二叉搜索树** — 二分取中间递归（区间`[i, j)`）。
```java
TreeNode dfs(int[] nums, int i, int j) {
    if (i == j) return null;
    int m = (i + j) / 2;
    return new TreeNode(nums[m], dfs(nums, i, m), dfs(nums, m+1, j));
}
```

### 视图 / 变体
**199. 二叉树的右视图** — ①BFS层序取最后一个 ②DFS前序先右后左，`depth==ans.size()`时添加。
```java
// DFS法
void dfs(TreeNode root, List<Integer> ans, int depth) {
    if (root == null) return;
    if (depth == ans.size()) ans.add(root.val);   // 该层第一次遇到
    dfs(root.right, ans, depth + 1);
    dfs(root.left, ans, depth + 1);
}
```

**230. 二叉搜索树中第K小的元素** — 中序遍历+计数`--k`，找到就提前返回。
```java
int dfs(TreeNode root) {
    if (root == null || k <= 0) return -1;
    int lRes = dfs(root.left);
    if (lRes != -1) return lRes;    // 左边找到就直接返回
    if (--k == 0) return root.val;
    return dfs(root.right);
}
```

**437. 路径总和III** — 前缀和+HashMap，`cur-prev=target` → `ans+=cnt.get(cur-target)`，注意回溯注销。
```java
Map<Long,Integer> cnt = new HashMap<>(); cnt.put(0L, 1);
int dfs(TreeNode root, long cur, int target, Map<Long,Integer> cnt) {
    if (root == null) return 0;
    cur += root.val;
    int ans = cnt.getOrDefault(cur - target, 0);
    cnt.merge(cur, 1, Integer::sum);
    ans += dfs(root.left, cur, target, cnt) + dfs(root.right, cur, target, cnt);
    cnt.merge(cur, -1, Integer::sum);   // 回溯注销该节点
    return ans;
}
```

---

## 八、回溯

### 选与不选模型
**78. 子集** — 每个元素选/不选，`dfs(i+1)`不选 → 选(add) → `dfs(i+1)` → 撤销。
```java
void dfs(int i, int[] nums, List<List<Integer>> ans, List<Integer> res) {
    if (i == nums.length) { ans.add(new ArrayList<>(res)); return; }
    dfs(i + 1, nums, ans, res);       // 不选
    res.add(nums[i]);
    dfs(i + 1, nums, ans, res);       // 选
    res.remove(res.size() - 1);       // 撤销
}
```

**39. 组合总和** — 完全背包的选与不选，选`dfs(i, target-x)`（可重复），不选`dfs(i+1, target)`。
```java
void dfs(int i, int target, int[] nums, List<List<Integer>> ans, List<Integer> res) {
    if (target == 0) { ans.add(new ArrayList<>(res)); return; }
    if (i == nums.length || target < 0) return;
    dfs(i + 1, target, nums, ans, res);     // 不选
    res.add(nums[i]);
    dfs(i, target - nums[i], nums, ans, res); // 选（可重复，i不变）
    res.remove(res.size() - 1);
}
```

**22. 括号生成** — 选与不选变种，`i<n`选`(`，`j<i`选`)`。
```java
void dfs(int i, int j, int n, char[] res, List<String> ans) {
    if (j == n) { ans.add(new String(res)); return; }
    if (i < n) { res[i+j] = '('; dfs(i+1, j, n, res, ans); }
    if (j < i) { res[i+j] = ')'; dfs(i, j+1, n, res, ans); }
}
```

**131. 分割回文串** — 选逗号`isPar[i..j]`则加入下一段从j+1开；不选逗号当前段扩一个。
```java
void dfs(int i, int j, char[] words, List<String> res, List<List<String>> ans) {
    if (j == words.length) { ans.add(new ArrayList<>(res)); return; }
    if (j < words.length - 1) dfs(i, j + 1, words, res, ans);  // 不选逗号
    if (isPar(words, i, j)) {                                   // 选逗号
        res.add(new String(words, i, j - i + 1));
        dfs(j + 1, j + 1, words, res, ans);
        res.remove(res.size() - 1);
    }
}
```

### 全排列模型
**46. 全排列** — 按位置填，`onPath`标记 + 回溯（选择→递归→撤销）。
```java
void dfs(int i, int[] nums, List<List<Integer>> ans, List<Integer> path, boolean[] onPath) {
    if (i == nums.length) { ans.add(new ArrayList<>(path)); return; }
    for (int j = 0; j < nums.length; j++) {
        if (!onPath[j]) {
            path.add(nums[j]); onPath[j] = true;       // 选择
            dfs(i + 1, nums, ans, path, onPath);        // 递归
            onPath[j] = false; path.remove(path.size()-1); // 撤销
        }
    }
}
```

**17. 电话号码的字母组合** — 全排列式DFS，遍历第一个集合，每个字符递归`dfs(i+1)`。
```java
void dfs(int i, List<String> ans, char[] res, char[] digits) {
    if (i == digits.length) { ans.add(new String(res)); return; }
    String letters = map[digits[i] - '0'];
    for (char c : letters.toCharArray()) { res[i] = c; dfs(i+1, ans, res, digits); }
}
```

### 二维DFS
**79. 单词搜索** — DFS找连通块+数值匹配，`vis`标记回溯。
```java
boolean dfs(int i, int j, boolean[][] vis, char[][] board, int k, char[] word) {
    if (i<0||i>=m||j<0||j>=n||vis[i][j]||board[i][j]!=word[k]) return false;
    if (k == word.length - 1) return true;
    vis[i][j] = true;
    for (int[] d : DIRS) if (dfs(i+d[0], j+d[1], vis, board, k+1, word)) return true;
    vis[i][j] = false;  // 回溯
    return false;
}
```

---

## 九、动态规划 (DP)

### 一维线性DP
**53. 最大子数组和** — `f[i+1]=max(f[i]+x, x)`。
```java
int[] f = new int[n+1]; int ans = MIN;
for (int i=0; i<n; i++) { f[i+1]=Math.max(f[i]+nums[i], nums[i]); ans=Math.max(ans,f[i+1]); }
```

**70. 爬楼梯** — `f[i+2]=f[i+1]+f[i]`（斐波那契）。
```java
int[] f = new int[n+1]; f[0]=f[1]=1;
for (int i=0; i<n-1; i++) f[i+2]=f[i+1]+f[i];
```

**198. 打家劫舍** — `f[i+2]=max(f[i]+x, f[i+1])`，不偷相邻。
```java
int[] f = new int[n+2];
for (int i=0; i<n; i++) f[i+2]=Math.max(f[i]+nums[i], f[i+1]);
return f[n+1];
```

**300. 最长递增子序列 (LIS)** — 枚举选哪个，`f[i]=max(f[j])+1 (j<i且nums[j]<nums[i])`。
```java
int[] f = new int[n]; int ans = 0;
for (int i=0; i<n; i++) {
    for (int j=0; j<i; j++) if (nums[j] < nums[i]) f[i] = Math.max(f[i], f[j]);
    ans = Math.max(ans, ++f[i]);
}
```

**152. 乘积最大子数组** — 维护`fMax`和`fMin`（正负号），取`max(fMax*x, fMin*x, x)`。
```java
int[] fMax=new int[n+1], fMin=new int[n+1]; fMax[0]=fMin[0]=1;
for (int i=0; i<n; i++) {
    int x = nums[i];
    fMax[i+1]=Math.max(Math.max(fMax[i]*x, fMin[i]*x), x);
    fMin[i+1]=Math.min(Math.min(fMax[i]*x, fMin[i]*x), x);
    ans = Math.max(ans, fMax[i+1]);
}
```

**5. 最长回文子串** — 中心扩展法，奇数1个中心，偶数2个中心。
```java
// 奇回文
for (int i=0; i<n; i++) {
    int l=i, r=i;
    while (l>=0 && r<n && s[l]==s[r]) { l--; r++; }
    if (r-l-2 > ansR-ansL) { ansLeft=l+1; ansRight=r-1; }
}
// 偶回文
for (int i=0; i<n-1; i++) {
    if (s[i]==s[i+1]) {
        int l=i, r=i+1;
        while (l>=0 && r<n && s[l]==s[r]) { l--; r++; }
        if (r-l-2 > ansR-ansL) { ansLeft=l+1; ansRight=r-1; }
    }
}
```

### 二维网格DP
**62. 不同路径** — `f[i+1][j+1]=f[i][j+1]+f[i+1][j]`。
```java
int[][] f = new int[m+1][n+1];
for (int i=0; i<m; i++)
    for (int j=0; j<n; j++)
        f[i+1][j+1] = i==0&&j==0 ? 1 : f[i][j+1]+f[i+1][j];
return f[m][n];
```

**64. 最小路径和** — `f[i+1][j+1]=min(f[i+1][j], f[i][j+1])+grid[i][j]`。
```java
int[][] f = new int[m+1][n+1];
Arrays.fill(f[0], Integer.MAX_VALUE/2);
for (int i=0; i<=m; i++) f[i][0] = Integer.MAX_VALUE/2;
f[1][0] = 0;
for (int i=0; i<m; i++)
    for (int j=0; j<n; j++)
        f[i+1][j+1] = Math.min(f[i+1][j], f[i][j+1]) + grid[i][j];
return f[m][n];
```

### 字符串DP
**1143. 最长公共子序列 (LCS)** — 相等`f[i+1][j+1]=f[i][j]+1`；不等`max(f[i+1][j], f[i][j+1])`。
```java
int m=t1.length(), n=t2.length();
int[][] f = new int[m+1][n+1];
for (int i=0; i<m; i++)
    for (int j=0; j<n; j++) {
        if (t1[i] == t2[j]) f[i+1][j+1] = f[i][j] + 1;
        else f[i+1][j+1] = Math.max(f[i+1][j], f[i][j+1]);
    }
return f[m][n];
```

**72. 编辑距离** — 相等`f[i+1][j+1]=f[i][j]`；不等`min(替,插,删)+1`。
```java
for (int i=0; i<m; i++)
    for (int j=0; j<n; j++) {
        if (s1[i]==s2[j]) f[i+1][j+1] = f[i][j];
        else f[i+1][j+1] = Math.min(Math.min(f[i][j], f[i+1][j]), f[i][j+1]) + 1;
    }
```

### 背包问题
**416. 分割等和子集** — 01背包，容量`sum/2`，`f[i+1][j]=f[i][j-x]`(选) || `f[i][j]`(不选)。
```java
int sum = ...; if (sum%2!=0) return false; sum/=2;
boolean[][] f = new boolean[n+1][sum+1]; f[0][0]=true;
for (int i=0; i<n; i++) {
    int x = nums[i];
    for (int j=0; j<=sum; j++)
        f[i+1][j] = j<x ? f[i][j] : f[i][j-x] || f[i][j];
}
return f[n][sum];
```

**322. 零钱兑换** — 完全背包，`f[i+1][j]=min(f[i+1][j-x]+1, f[i][j])`。
```java
int[][] f = new int[n+1][amount+1];
Arrays.fill(f[0], MAX/2); f[0][0]=0;
for (int i=0; i<n; i++) {
    int x = coins[i];
    for (int j=0; j<=amount; j++)
        f[i+1][j] = j<x ? f[i][j] : Math.min(f[i+1][j-x]+1, f[i][j]);
}
```

**279. 完全平方数** — 完全背包，物品是`i*i`。同类思想：**322**。
```java
int target = (int)Math.sqrt(n);
int[][] f = new int[target+1][n+1];
Arrays.fill(f[0], MAX); f[0][0]=0;
for (int i=1; i<=target; i++)
    for (int j=0; j<=n; j++)
        f[i][j] = j<i*i ? f[i-1][j] : Math.min(f[i-1][j], f[i][j-i*i]+1);
return f[target][n];
```

### 其他
**118. 杨辉三角** — `f[i][j]=f[i-1][j]+f[i-1][j-1]`。
```java
List<List<Integer>> ans = new ArrayList<>();
ans.add(Arrays.asList(1));
for (int i=1; i<numRows; i++) {
    List<Integer> res = new ArrayList<>(); res.add(1);
    for (int j=1; j<i; j++) res.add(ans.get(i-1).get(j) + ans.get(i-1).get(j-1));
    res.add(1);
    ans.add(res);
}
```

---

## 十、数学 / 贪心

### 贪心
**121. 买卖股票的最佳时机** — 贪心维护最小值`minC`，`ans=max(x-minC)`。
```java
int minC = nums[0], ans = 0;
for (int x : nums) { minC = Math.min(minC, x); ans = Math.max(ans, x - minC); }
```

**55. 跳跃游戏** — 贪心维护最远可达范围`maxReach`。

**45. 跳跃游戏II** — 贪心，记录最远距离，到右边界`curEnd`时更新并`ans++`。
```java
int maxReach=0, ans=0, curEnd=0;
for (int i=0; i<n-1; i++) {
    maxReach = Math.max(maxReach, i+nums[i]);
    if (i == curEnd) { curEnd = maxReach; ans++; }
}
```

**763. 划分字母区间** — 先拿所有字母的最后下标，枚举更新右边界，`end==i`时切分。
```java
int[] last = new int[26];
for (int i=0; i<s.length; i++) last[s[i]-'a'] = i;
int start=0, end=0;
for (int i=0; i<s.length; i++) {
    end = Math.max(end, last[s[i]-'a']);
    if (end == i) { ans.add(end-start+1); start = i+1; }
}
```

### 位运算 / 投票
**136. 只出现一次的数字** — 异或`ans^=x`，相同抵消。
```java
int ans = 0; for (int x : nums) ans ^= x; return ans;
```

**169. 多数元素** — 摩尔投票HP计数器，相同+1不同-1，HP归零换候选。
```java
int hp=1, cur=nums[0];
for (int i=1; i<n; i++) {
    if (hp == 0) cur = nums[i];
    if (nums[i] == cur) hp++; else hp--;
}
return cur;
```

---

## 十一、图

**200. 岛屿数量** — DFS/BFS遍历连通块，`vis`标记，遇到`'1'`未访问`ans++`并扩散。
```java
// DFS
void dfs(char[][] grid, int i, int j, boolean[][] vis) {
    if (i<0||i>=m||j<0||j>=n||vis[i][j]||grid[i][j]!='1') return;
    vis[i][j] = true;
    for (int[] d : DIRS) dfs(grid, i+d[0], j+d[1], vis);
}
// 主循环: if(grid[i][j]=='1'&&!vis[i][j]) { dfs(...); ans++; }
```

**207. 课程表** — 三色标记法拓扑排序找环：0未访问，1正在访问(有环)，2访问结束。
```java
int[] color = new int[n];
boolean dfs(int i, List<Integer>[] g, int[] color) {
    color[i] = 1;
    for (int x : g[i])
        if (color[x]==1 || color[x]==0 && dfs(x, g, color)) return true; // 有环
    color[i] = 2;
    return false;
}
```

**994. 腐烂的橘子** — BFS多源扩散，初始烂橘子入队，统计新鲜橘子数，每轮扩散减一。
```java
int[][] vis = new int[m][n];
Queue<int[]> q = new ArrayDeque<>();
for (i,j) if(grid[i][j]==2) q.offer({i,j}); else if(grid[i][j]==1) { vis[i][j]=-1; fresh++; }
while (!q.isEmpty()) {
    int[] cur = q.poll();
    for (int[] d : DIRS) {
        int x=i+d[0], y=j+d[1];
        if (x>=0&&y>=0&&x<m&&y<n && vis[x][y]==-1) {
            vis[x][y]=vis[i][j]+1; fresh--; q.offer({x,y});
            ans=Math.max(ans, vis[x][y]);
        }
    }
}
return fresh==0 ? ans : -1;
```

---

## 十二、矩阵 / 设计

**48. 旋转图像** — 先转置(`matrix[i][j]↔matrix[j][i]`)，再行反转。
```java
for (int i=0; i<n; i++)           // 转置
    for (int j=0; j<i; j++) swap matrix[i][j], matrix[j][i];
for (int[] row : matrix)           // 行反转
    for (int j=0; j<n/2; j++) swap row[j], row[n-1-j];
```

**54. 螺旋矩阵** — 方向标记法，`DIRS[4]`循环，出界/已访问则右转`di=(di+1)%4`。
```java
int[][] DIRS = {{0,1},{1,0},{0,-1},{-1,0}};
int i=0, j=0, di=0;
for (int k=0; k<total; k++) {
    ans.add(matrix[i][j]); matrix[i][j] = -1;   // 标记已访问
    int x=i+DIRS[di][0], y=j+DIRS[di][1];
    if (x<0||x>=m||y<0||y>=n||matrix[x][y]==-1) di = (di+1)%4; // 右转
    i += DIRS[di][0]; j += DIRS[di][1];
}
```

**240. 搜索二维矩阵II** — 从右上角开始，`>target`左移，`<target`下移。
```java
int i=0, j=n-1;
while (i<m && j>=0) {
    if (matrix[i][j] == target) return true;
    if (matrix[i][j] > target) j--; else i++;
}
```

---

## 十三、排序

**912. 排序数组** — 快排：随机pivot → 相向双指针分区 → 递归左右。
```java
void sortQuick(int[] nums, int l, int r) {
    if (l >= r) return;
    int i = partition(nums, l, r);
    sortQuick(nums, l, i-1); sortQuick(nums, i+1, r);
}
int partition(int[] nums, int l, int r) {
    int i=l+new Random().nextInt(r-l+1), pivot=nums[i]; swap(nums,i,l);
    i=l+1; int j=r;
    while (true) {
        while (i<=j && nums[i]<pivot) i++;
        while (i<=j && nums[j]>pivot) j--;
        if (i>j) break;
        swap(nums, i++, j--);
    }
    swap(nums, l, j); return j;
}
```

**215. 数组中的第K个最大元素** — 快排分治（只用分区），每次partition后判断pivot与`n-k`关系，只递归一侧。
```java
int kIndex = n - k, l=0, r=n-1;
while (true) {
    int i = partition(nums, l, r);
    if (i == kIndex) return nums[i];
    if (i > kIndex) r = i-1; else l = i+1;
}
```

**347. 前K个高频元素** — 桶排序：HashMap统计频率 → 频率为下标建桶 → 倒序遍历取TopK。
```java
Map<Integer,Integer> map = new HashMap<>();
for (int x : nums) map.merge(x, 1, Integer::sum);
int maxCnt = Collections.max(map.values());
List<Integer>[] buckets = new List[maxCnt+1];
Arrays.setAll(buckets, i->new ArrayList<>());
for (var e : map.entrySet()) buckets[e.getValue()].add(e.getKey());
for (int i=maxCnt; j<k; i--)
    for (int x : buckets[i]) ans[j++] = x;
```

---

## 相似解法速查

| 核心思想 | 题目 |
|---------|------|
| HashMap映射 | P1, P49, P56 |
| 前缀和+HashMap | P560, P437 |
| 前后缀分解 | P238, P42(法一) |
| 滑动窗口 | P3, P438, P76 |
| 单调队列 | P239 |
| 二分 lowerBound | P34, P33, P74 |
| 旋转数组二分 | P153, P33 |
| 中位数二分 | P4 |
| 单调栈 | P739, P84, P42(法三) |
| 辅助栈 | P20, P155, P394 |
| 快慢指针 | P141, P142, P148, P234 |
| 反转链表 | P206, P25, P24 |
| 链表归并 | P21, P23, P148 |
| 选与不选(回溯) | P78, P39, P22, P131 |
| 全排列(回溯) | P46, P17 |
| 二维DFS | P79 |
| 一维线性DP | P53, P70, P198, P300, P152 |
| 网格DP | P62, P64 |
| 字符串DP(LCS/编辑) | P1143, P72 |
| 01背包 | P416 |
| 完全背包 | P322, P279, P39(回溯) |
| 中心扩展 | P5 |
| 贪心更新边界 | P121, P55, P45, P763, P11 |
| BFS层序(树) | P102, P199 |
| DFS遍历(树) | P94, P98, P101, P104, P226, P236, P543 |
| 构造树(分治) | P105, P108 |
| 图DFS/BFS | P200, P207, P994 |
| 快排分治 | P912, P215 |
| 桶排序 | P347 |
| 位运算/投票 | P136, P169 |
